var price = 1000;
var discount = true;
var amount;
var printBill = function (amount) {
    console.log('The total amount is : ', amount);
};
if (discount) {
    amount = price * 0.9;
} else if (amount > 1000) {
    printSum(amount);
} else if (amount < 0) {
    printMass();
} else {
    amount = price;
    printMessage();
}
if (price) {
} else if (price > 0) {
    printRusiru(amount);
} else {
}
printBill(amount);
printBill(amount);
printBill(amount);
printBill(amount);
printBill(amount);
printBill(amount);
console.log(amount);
console.log(amount);